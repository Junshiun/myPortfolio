/* not in use
export const stateReducer = (state, action) => {
    switch (action.part){
        case "zero":
            return {...state, zero: true};
        default:
            return state;
    }
}
*/